# 핸드오프 — 같교오락실 포털 (2026-08-14 세션)

## 1. 이 프로젝트가 뭐 하는 앱인지

같이교육이 만든 앱 4개(QUIZ TOGETHER / CLASSCADE / AI Ways Incheon / Be a Googler)를
골라서 체험하는 전시용 포털. 단일 정적 `index.html` + `assets/`, 빌드 과정 없음.
`edutogether.kr`에서 GitHub Pages로 서비스 중 (repo: `edutogether/portal`).

## 2. 확인/진단한 내용

- **모바일 스크롤 버그(근본원인)**: `justify-content:center`가 뷰포트보다 큰 콘텐츠를
  화면 위 음수좌표로 밀어내는 flexbox 함정 때문에 카드1이 영구히 스크롤 도달 불가였음.
- **버벅임 원인 진단**: `backdrop-filter`(카드4+플레이어=5곳) + 애니메이션 오브 39개
  조합이 렌더링 부담을 줄 것으로 가설 세우고 오브 대부분을 정적으로 전환함. **다만
  실제 FPS 수치로 확정하진 못했음** — 이 자동화 브라우저 탭이 `hasFocus:false`/
  `visibilityState:hidden` 상태라 rAF 기반 측정이 막혀서 방법론적으로 검증 불가였음.
  사용자 실제 화면에서 체감 개선됐는지는 미확인.
- **내부 Browser 툴 폐기**: 스크린샷이 세션 내내 실패해서 Claude in Chrome(실제 크롬)으로
  전환. 그 과정에서 로컬 테스트 서버가 HTTP Range 요청을 지원 안 해 실제 크롬에서
  오디오 재생이 아예 안 되는 **진짜 버그**를 발견·수정함 (내부 브라우저에선 안 걸렸던 문제).

## 3. 완료한 것 (전부 라이브 배포·태그 완료)

- 모바일 스크롤 버그 수정 (`margin:auto` 방식, 데스크탑 중앙정렬은 그대로 유지)
- **PC 데스크탑(>1180px) 전용 뮤직 플레이어** 신규 구현 — 좌측 플레이어 + 우측 카드 2x2
  - 재생/일시정지/이전/다음/셔플/반복/음소거/볼륨, `PLAYLIST` 배열 구조(현재 1곡, 확장 대비)
  - 가사: 카라오케 좌→우 채우기 + 줄 단위 등속 연속 스크롤(겹침 방지 로직 포함)
  - 자동재생 + 정책상 막히면 첫 상호작용 시 재생하는 폴백
  - 곡: 산울림 "개구장이"(앨범명 표기), 1절 마지막 가사만 실제 발음 "개구쟁이"로 표기
- 아이콘을 애플뮤직풍 둥근 스타일로 재작업
- **"8.14 일본군 '위안부' 피해자 기림의 날"** 추모 문구(나눔명조, 헤더 상단 중앙)
- 나비 배경 이미지 + CLASSCADE의 반딧불이 효과(`moteHash` 방식, 100개) 포팅 —
  **CLASSCADE 원본 코드는 참고만 하고 손대지 않음**
- 카드 1~4 반투명 유리 효과(backdrop-filter) + 배경 원 배치, 카드/플레이어 테두리 통일 제거
- 즐겨찾기 기본 켜짐, "더보기" 클릭 시 안내 토스트(자동 소멸)
- git: 커밋 다수 + 프리즈 태그 3개(`portal-freeze-20260813` 원본,
  `portal-freeze-20260814`, `portal-freeze-20260814b`) — 전부 푸시 완료, 작업트리 클린

## 4. 아직 안 끝난 것 / 다음에 이어서 할 것

- **가사 타임스탬프**: 각 절의 1~4번째 줄(전주부)은 여전히 근사치. 5~7번째·2절 실측
  구간은 사용자가 초 단위로 직접 준 값이라 정확함. 더 정확히 하려면 사용자한테
  나머지 줄 시작/끝 초를 요청하면 됨.
- **버벅임 실측 검증**: 위 2번 항목대로 가설 기반 수정만 했고, 사용자 실제 화면에서
  진짜 개선됐는지 재확인 필요.
- **나비 배경 이미지**: 세션 중 여러 번 이미지 교체 요청이 있었음(단순화·따뜻한 톤
  방향으로). 현재 버전이 최종이지만 사용자가 더 다듬고 싶어할 수 있음.
- Safari/Firefox 크로스브라우저 테스트 전혀 안 됨 — Windows 환경이라 Safari는 물리적으로
  테스트 불가(사용자가 맥/아이폰으로 확인해야 함), Firefox도 자동화 불가(사용자가 직접).

## 5. 주의사항

- **프리즈 태그 3개 존재 — 옮기지 말 것.** 큰 변경 후엔 항상 새 날짜 태그를 만들 것
  (`.githooks/pre-push`가 `*-freeze-*` 태그 삭제·강제이동을 차단함).
- **앱 4개 코드는 이 저장소에서 절대 수정 안 함** — 포털은 링크만 검. 앱 수정이 필요하면
  해당 앱 저장소 세션으로 넘길 것.
- `assets/bg-loading.webp`는 미사용이지만 재사용 대비 의도적 보존 — 지우지 말 것.
- 로딩 애니메이션에 `prefers-reduced-motion` 정지 없음 — 사용자가 명시적으로 제거 요청한
  것이므로 되살리지 말 것.
- `.claude/`(로컬 정적 서버 `static-server.js` + `launch.json`)는 `.gitignore` 처리됨,
  로컬 전용 도구라 커밋 대상 아님. 로컬 재현하려면
  `node .claude/static-server.js` 실행 후 `http://localhost:4319`.
- `edutogether.kr` 도메인은 원래 CLASSCADE 소유였다가 포털로 이전된 것 — CLASSCADE 쪽에서
  다시 커스텀 도메인을 claim하면 포털이 깨짐.
- 배포 확인 시 HTTP 200만 보지 말고 실제 DOM/자산 로드까지 확인할 것(상위 CLAUDE.md 원칙).
