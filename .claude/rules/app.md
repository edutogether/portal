# portal (같교오락실) 개별 규칙

헌법(D:\Projects\CLAUDE.md → _shared/CONVENTIONS.md)에 없는 것만.

## 앱
- 무엇: 같이교육 앱 6종으로 링크를 거는 전시용 포털. React + TypeScript + Vite.
- 사용자: 학생·교사. 상시 공개돼 있으나 실제 사용은 교육청 행사 중심.
- 배포: Firebase Hosting (프로젝트 `edutogether-portal`, 계정
  `edutogether2015@gmail.com` — `817beatles@gmail.com`엔 접근 권한 없음).
  Functions 없음(순수 정적이라 §4.1대로 두지 않음).

## 배포 폴더
- `firebase.json` public = `dist`(빌드 산출물). `src/`, `_docs/`, `.claude/`,
  `scripts/`, `tests/`, `.github/`가 여기 포함되지 않음을 확인함 (확인일 9/8)

## 데이터
- 개인정보·미성년자 데이터: **없음**. 폼·로그인·쿠키·분석 스크립트·백엔드가 전부 없고,
  CSP `connect-src 'self'`로 외부 송신 경로도 정책상 막혀 있다.
- 보관·삭제 정책: 해당 없음(수집하는 데이터 자체가 없음).
- rules: firestore.rules / storage.rules **의도적 생략** — 클라이언트가 접근하는
  Firebase 서비스가 Hosting뿐이고 Firestore·Storage를 아예 쓰지 않는다.

## 이 앱에서 절대 하면 안 되는 것
- **6개 앱의 코드를 이 저장소에서 수정하는 것.** 포털은 링크만 건다. 앱 수정이
  필요하면 그 앱 저장소 세션으로 넘긴다.
- **앱 링크를 같은 탭으로 여는 것.** 전부 `target="_blank" rel="noopener noreferrer"`
  여야 한다 — 이 구조 덕분에 각 앱에 "포털로 돌아가기" 버튼을 넣지 않아도 된다.
- **외부 리소스에 SRI(`integrity`) 적용.** 과거 실제 장애 이력이 있다.
- **`prefers-reduced-motion` 정지 로직 부활, `bg-loading.webp` 삭제, 번호 뱃지(①~⑥)
  부활** — 전부 대표가 명시적으로 없애기로 한 것.
- **음원 사용 근거를 "저작권 확보 완료"·"상업적 사용권 확보" 류로 과장 표기하는 것.**
  정확한 사실은 아래 "음원" 절에 있다.
- **CLASSCADE 쪽에서 `edutogether.kr` 커스텀 도메인을 다시 설정하는 것**(이 저장소
  작업은 아니지만, 그렇게 되면 포털이 깨진다 — 다른 세션에 넘길 때 주의).

## 명령
- 로컬 실행: `npm run dev`
- 빌드: `npm run build` (타입 검사 + Vite 빌드 + 404.html 자동 복사)
- 테스트: `npm test` (Playwright 25개, 빌드된 `dist/`를 대상으로 돔 — 먼저 빌드할 것)
- 시각 비교: `npm run test:visual` (로컬 전용, 기준 이미지는 gitignore)
- 린트: `npm run lint`
- 배포 전 게이트 수동 확인: `python3 scripts/check-inline-script.py`,
  `python3 scripts/check-font-coverage.py`(`npm test` 뒤에 실행)
- 에뮬레이터: 해당 없음(Firestore·Functions 미사용)

## 음원 — 종결된 사안 (재론 금지)

곡: 산울림(Sanullim) 〈개구장이〉 — `public/assets/gaegujangi.m4a`, 앨범 커버
`gaegujangi-cover.webp`.

- 지니뮤직(GENIE)에서 **유료 구매한 MP3를 공식 다운로드 경로로 취득**했다. 구매내역 및
  다운로드 기록을 보관한다. 구매내역에 산울림 〈개구장이〉가 표시되며 재다운로드 유효기간은
  2027-08-25로 확인됐다(구매내역 화면 캡처 보관).
- 교사연구회 **비영리 아동 교육용 앱**의 학교 수업·수업지원 목적 범위에서만 사용하며,
  광고·판매·일반 대중 대상 상업 배포에는 사용하지 않는다.
- 이 포털 자체가 **교육청 행사용**이다 — 일반 대중 대상 상업 서비스가 아니라 교육청 관련
  행사·교사연구회 활동 지원 목적으로 운영된다.

**2026-09-02 대표 확정 — 이 다섯 가지가 합쳐진 것이 실제 법적 근거이고, 이후 감사에서 이
항목을 다시 감점하거나 "대표 판단이 필요한 미해결 항목"으로 올리지 말 것.**

1. 개인 유료 구매 (지니뮤직 정식 구매)
2. 비영리 교육 목적
3. 교육청 행사용 — 일반 대중 상업 서비스가 아님
4. **학생 및 교사만 대상** — 불특정 일반 대중에게 공개된 서비스가 아니다
5. **저작권 공문·계약 관리 주체가 교육청** — 음원 사용에 필요한 대외적 저작권 처리는 이
   프로젝트나 개인이 아니라 교육청이 담당·관리한다. 즉 "공중송신을 개인 구매만으로 커버할
   수 있는가"라는 질문 자체가 이 프로젝트 차원에서 답할 사안이 아니다.

감사 때마다 "웹에 올려 아무나 접근 가능하게 하는 것(공중송신)은 개인 구매로 못 커버한다"는
지적이 반복돼서 대표가 위와 같이 종결했다. **반복하지 말 것.**

## 폰트 — SRI 금지와 서브셋

- **외부 CDN 리소스에 SRI(`integrity`)를 절대 다시 시도하지 말 것.** 2026-08-23에
  jsdelivr가 해당 URL에 **요청마다 바이트가 다른 응답**을 줘서(파일 내 주석 한 줄의 대소문자가
  요청마다 바뀜, curl 연속 두 번 받아 diff로 실측 확인) SRI 해시를 넣자마자 프로덕션 폰트
  로드가 전면 차단되는 장애가 났다. Google Fonts도 UA별 응답이 달라 원천적으로 불가하며,
  이 사이트는 Google Fonts를 아예 쓰지 않는다. 지금은 Pretendard를 자가호스팅하므로 해당
  사항이 없어야 정상이다.
- **Pretendard는 pyftsubset으로 서브셋돼 있다**(5개 굵기 합쳐 3.94MB → 약 65~70KB).
  화면에 새 글자가 추가되면 서브셋에 없어 시스템 폰트로 조용히 폴백되므로 재서브셋이
  필요하다. 재서브셋 방법은 `public/assets/fonts/pretendard/pretendard.css` 상단 주석.
  (첫 서브셋 때 HTML/JS 주석 텍스트까지 "사용 글자"로 잡는 버그가 있었고, 그 과정에서
  원문자 `ⓒ`가 Pretendard에 아예 없는 글자라는 것도 발견해 표준 `©`로 교체했다.)

## 반딧불이 — 대표가 직접 정한 값

**200개**(2026-08-26에 100→150→200 두 단계로 대표가 직접 증량), 경로 애니메이션 3종
(`mote-drift`/`-b`/`-c`) 균등 순환, **delay는 각 입자 자신의 dur에 비례해서** 뽑는다.

원래는 애니메이션 1종에 delay가 dur와 무관한 고정 범위였는데, 대표가 라이브 화면에서
"같은 시기에 우르르 올라갔다 내려온다"고 지적해 위상을 흩어지게 바꿨다. **delay를 dur에
비례시키지 않고 고정 범위로 뽑으면** 주기가 짧은 입자들이 한 바퀴를 여러 번 돌아 결국 다시
비슷한 위상끼리 뭉친다 — 이 방식으로 되돌리지 말 것.

## CSP — 지키는 대상이 바뀐 것이지 없어진 게 아니다

`script-src`는 2026-09-08부터 `'self'`다. 리액트 전환으로 스크립트가 전부 외부 파일로
빠져 인라인 스크립트가 남지 않았고, 해시를 맞출 대상 자체가 사라졌다(그 전 이력:
2026-08-25 첫 시도 실패 → 2026-09-01에 원인 규명 후 재적용 — `<script>` 태그 바로 뒤 첫
줄바꿈까지 포함해 해시해야 한다는 것).

**검사는 없애지 않고 지키는 대상을 바꿨다**: `scripts/check-inline-script.py`가 산출물에
인라인 `<script>`가 생기면 CI를 실패시킨다 — `'self'`만으로는 인라인 스크립트가 **조용히**
차단되기 때문이다(예전 해시 불일치와 똑같은 증상). `style-src`의 `'unsafe-inline'`은
유지해야 한다: 반딧불이가 입자마다 인라인 CSS 변수를 설정한다.

## 소리 — 두 번 뒤집힌 결정이라 배경까지 남긴다

**지금 사양(2026-09-09 대표 확정)**: 방문하면 **소리가 난다.** 볼륨은 0에서 50%까지
부드럽게 올라오고, 음소거를 껐다 켜면 **직전 값**으로(55%였으면 55%로) 부드럽게
돌아온다. 볼륨바도 그 흐름을 따라 움직인다.

- **2026-08-31 (`3c8a48e`)**: 대표가 "소리가 저절로 나온다"고 지적 → `audio.muted = true`를
  강제해 **항상 음소거로 시작**하게 바꿨다. 브라우저 자동재생 정책이 방문 이력에 따라
  달라져서 사람마다 소리가 나기도 안 나기도 하는 게 이유였다.
- **2026-09-09**: 대표가 이 결정을 **직접 뒤집었다** — "사람이 의도적으로 주소를 치거나
  북마크된 걸 눌러서 들어갈 땐 무조건 소리가 나야 돼. 50% 볼륨으로 해서 시작해."

그래서 **"음소거로 시작하는 게 맞지 않나"라고 되돌리지 말 것.** 이미 한 번 그쪽이었고
대표가 뒤집은 상태다.

구현에서 지켜야 하는 것:

- **`audio.muted`를 쓰지 않는다.** 소리는 `volume` 하나로만 다루고, 음소거 = volume 0이다.
  둘을 같이 쓰면 "조용함"의 출처가 둘이 되어(muted=true인데 volume은 0.5) 볼륨바가 실제
  상태와 어긋난다.
- **볼륨 토글 판정은 순간값이 아니라 페이드가 향하는 값으로 한다.** 0으로 내려가는 도중에
  다시 누르면 순간값(0.004)을 보고 "아직 소리가 있다"로 잘못 읽어서 직전 볼륨이 그 찰나의
  값으로 덮인다(실제로 겪음).
- **자동재생이 정책으로 거부되면**(첫 방문자에게 흔하다) 페이지 안에서의 아무 조작에나
  재생을 시작하되, **앱 카드를 눌러 바깥으로 나가는 클릭은 제외한다.** 과거 사고가 정확히
  이것이다 — 카드가 새 탭으로 열리는 동시에 방금 떠난 원래 탭에서 음악이 터졌다.
  `tests/autoplay-blocked.spec.js`가 이 예외를 회귀 테스트로 고정하고 있다.
- 플레이어 컨트롤(재생·볼륨·음소거)은 전역 조작 리스너보다 **먼저** 판단해야 한다.
  안 그러면 음소거를 눌렀는데 전역 리스너가 곧바로 재생을 걸어 사용자의 조작을 덮어쓴다.

## 자주 틀리는 것
(헌법의 "이력"이 아니라, 실제로 두 번 이상 반복된 함정 목록)

- **`backdrop-filter`를 표준 속성 먼저, `-webkit-` 접두사판을 나중에 쓰는 것.**
  순서가 그러면 빌드 미니파이어가 표준 속성을 지우고 접두사판만 남기는데, 현대 크롬은
  접두사판을 적용하지 않아 **블러가 조용히 사라진다.** 반드시 `-webkit-`을 먼저,
  표준을 나중에. (2026-09-08 리액트 전환 중 실제로 겪었고, 스크린샷 비교가 잡았다.)
- **CSS 소스 순서에 기대는 것.** 같은 선택자·같은 속성이면 파일에서 나중에 나오는
  규칙이 이긴다 — 앞엣것이 매칭되는 `@media` 안에 있어도 마찬가지다. 전환 전에는 이
  성질에 기대어 모바일 override를 특정 위치에 끼워넣는 구조였고, 그 때문에 규칙이
  조용히 무시되는 사고가 두 번 났다(`.app`/`.player-mobile`, `.desc .short`).
  지금은 **한 CSS 파일이 한 컴포넌트의 클래스를 전부 소유하고, 반응형 규칙도 그 파일
  안에 둔다**는 규칙으로 순서 의존을 없앴다 — 같은 클래스를 두 파일에서 손대지 말 것.
- **폰트 서브셋 재생성을 빠뜨림.** 새 문구를 추가하면 서브셋에 없는 글자가 시스템
  폰트로 조용히 폴백된다. 검사 대상 글자는 **렌더된 DOM**에서 뽑는다 — 소스를 훑으면
  코드 식별자까지 사용 글자로 잡히기 때문이다. (전환 전 검사는 정적 HTML만 봐서
  곡 길이 표시 `2:05`의 숫자가 빠진 걸 못 잡고 있었다.)
- **화면에 안 보이는 값을 다른 값에서 조립하는 것.** 카드 썸네일의 대체 텍스트(alt)를
  카드 제목에서 조립했다가, QUIZ TOGETHER만 "같이교육 퀴즈 미리보기"에서
  "QUIZ TOGETHER 미리보기"로 바뀐 채 배포된 적이 있다(2026-09-08, 감사에서 발견).
  **화면에 그려지지 않는 값이라 스크린샷 비교도 computed style 대조도 못 잡는다.**
  대체 텍스트·aria-label 같은 값은 데이터에 그대로 두고 그대로 쓸 것. 지금은
  `tests/locked-geometry.spec.js`가 여섯 개 값을 문자열로 고정한다.
- **테스트 서버가 검증하려는 것을 실제로 지원하는지 확인하지 않는 것.** 테스트 서버를
  파이썬 내장 서버로 쓰던 동안 **HTTP Range를 지원하지 않아 오디오가 아예 탐색 불가**
  (`audio.seekable`이 비어 있어 `currentTime` 설정이 조용히 무시됨)였고, 그래서 탐색
  동작을 바꿔놓고도 검증할 수단이 없었다(2026-09-09 발견). 지금은 `vite preview`를
  쓴다 — Range를 지원하고(206 Partial Content 실측) 배포 산출물을 그대로 서빙한다.
  `--host 127.0.0.1`은 유지할 것(넓게 열면 테스트 도는 동안 산출물이 외부에 노출된다).
- **화면이 바뀌지 않았는지 확인하지 않고 리팩터링하는 것.** 큰 변경 전에
  `npm run test:visual -- --update-snapshots`로 기준선을 만들어두고, 변경 후 비교한다.
  `tests/locked-geometry.spec.js`는 수치로도 같이 지킨다.
- **지시를 넘겨짚어 문장을 새로 짓는 것.** 문구 수정 지시는 받은 그대로만 반영한다.
  "자연스럽게 줄여달라"는 말을 임의로 문장을 다시 쓰거나 `<br>`을 새로 넣어도 된다는
  뜻으로 해석했다가 전체를 되돌린 사고가 있었다(2026-09-06). 구조를 유지하라는 말이
  있으면 그 구조를 그대로 두고 최소 변경만 한다.
- 느낌표·물음표 앞 띄어쓰기(`시간 !`)는 대표 표준 표기다. 새 문구에도 지킬 것.
- **검증용으로 수동으로 띄운 `vite preview` 서버를 끄지 않고 두는 것.** 2026-09-09에 임의 포트(4321/4337 등)로 여러 번 확인용 서버를 띄우고 끄지 않아, 다른 저장소(Voice Cinema)의 Playwright가 기존 서버를 재사용하는 설정 때문에 그 포트에 붙어서 E2E 12개가 전부 실패했다(팀장 경유로 발견). **`npm test`가 자동으로 띄우는 서버는 테스트가 끝나면 스스로 정리되지만, 확인차 손으로 띄운 `vite preview`는 직접 꺼야 한다.** 작업 중 서버를 수동으로 띄웠으면 검증이 끝나는 대로 반드시 종료하고, 보고에도 그 사실을 한 줄 남길 것.
- **큰 문서 정리 때 "유실 검사"를 미리 정한 키워드 목록으로만 하는 것.** 2026-09-09에
  CLAUDE.md를 226→102줄로 줄이며 키워드 grep으로 유실 검사를 했다고 보고했는데,
  `sync-check.yml`/`player-smoke-test.yml`이라는 두 워크플로우 이름이 애초에 그
  목록에 없어서 실제로 사라진 내용을 통과시켰다(§19 배경작업 중 발견해 복구,
  `3d088c0`). **목록 기반 검사는 "목록에 없는 것은 검사 대상이 아니라는 것" 자체를
  놓친다** — 목록을 짤 때부터 빠뜨린 항목은 영원히 안 잡힌다. 큰 구조 변경(문서
  분량을 크게 줄이거나 여러 파일로 쪼갤 때) 뒤에는 옮긴 문서와 원본을 통째로
  diff하거나(`git diff` 또는 옛 버전과의 `diff`), 적어도 원본에 있던 고유명사(파일명·
  워크플로우 이름·커밋 해시)를 전부 자동으로 뽑아 하나씩 대조하는 방식을 쓸 것 —
  사람이 "이건 중요하니 넣어야지" 하고 고른 키워드만으로는 안 된다.
- **CI에서 흔들리는(가끔 실패하는) 테스트를 여유 시간부터 늘려서 덮는 것.** 여유
  시간을 늘리는 건 증상을 지우는 것이지 원인을 없애는 게 아니고, 진짜 회귀가 났을
  때도 그만큼 늦게(혹은 운 좋게 타이밍이 맞아 아예 안) 잡힐 수 있다. **원인을 먼저
  좁히고, 재현이 안 되면 무엇을 시도했는지 기록하고 넘어갈 것.**
  - 2026-09-09에 `tests/autoplay-blocked.spec.js`의 카드 클릭 테스트가 CI에서 딱
    한 번(`3d088c0` 배포 시, 재실행하니 바로 통과) `waitForEvent('popup')` 15초
    타임아웃으로 실패했다. `#loader`가 `z-index:60`으로 화면 전체를 덮고, 자산이
    안 오면 최대 8초(`SAFETY_MS`)까지 떠 있을 수 있는데, 이 테스트는 로더가 끝나길
    기다리지 않고 바로 카드를 클릭했다 — 로더가 카드를 가리고 있으면 `click()`이
    내부적으로 계속 재시도하다가 테스트 전체 타임아웃(15초)을 넘길 수 있다.
  - 로더 자산을 인위로 7.8초 지연 + CDP로 CPU 4배 스로틀(CI 자원 경합 흉내)을
    걸어 재현을 시도했더니, 여러 번 돌려도 13.3~13.7초로 **15초 턱밑까지는 갔지만
    완전한 실패(타임아웃 초과)까지는 재현하지 못했다.** 즉 정확한 방아쇠는 못
    잡았지만, 여유가 매우 얇다는 것과 로더-클릭 순서가 근본 위험이라는 건 확인했다.
  - **그 CI 실행엔 trace/스크린샷이 전혀 안 남아 있어서**(당시 `playwright.config.js`에
    `trace` 설정이 없었다) 실측 증거 없이 추측에만 의존해야 했다. 그래서 시간을
    늘리는 대신 두 가지를 고쳤다: ① 이 테스트가 로더가 끝나길 먼저 기다리게 함(실제
    방문자도 로더가 떠 있는 동안은 카드를 못 누르니 이게 맞는 순서이기도 하다) ②
    `trace: 'retain-on-failure'`를 켜고 `deploy.yml`에 실패 시 trace를 아티팩트로
    올리는 스텝을 추가 — **다음에 또 흔들리면 이번처럼 추측만 하지 않고 실제
    증거(trace.zip)를 볼 수 있다.**
  - **바로 다음 배포(`f16d382`)에서 CI가 또 흔들렸다 — 이번엔 다른 테스트.**
    `player.spec.js`의 "음소거를 껐다 켜면 직전 볼륨으로 돌아온다"가
    `expect(volume).toBeCloseTo(0.55)`에서 `0.5`(정확히 `RESTING_VOLUME`)를 받아
    실패했다. 이번엔 **trace가 실제로 남아서**(바로 위에서 켠 것) 실측 증거로
    조사했다 — 페이지 스냅샷에 볼륨 슬라이더가 `"0.5"`로 찍혀 있어, `lastVolume`이
    직전 값(0.55)이 아니라 초기값(0.5)에 머물러 있는 것처럼 보였다.
  - **`toggleMute`/`setVolume` 로직을 의심해 실제 테스트와 같은 폴링 방식으로
    로컬에서 15번 왕복시켜봤지만 15번 다 정상이었다** — 재현 실패. 그 과정에서
    처음 만든 진단 스크립트가 `waitForFunction(volume > 0.5)`로 판정했는데, 목표값
    0.55가 0.5를 지나며 올라가는 페이드 중간에 통과해버려서 **가짜 불일치를
    스스로 만들어낸 것**이었다(진짜 테스트가 쓰는 `toBeCloseTo` 폴링으로 바꾸니
    사라졌다) — 진단 코드 자체의 결함으로 없는 버그를 볼 뻔한 사례로 남긴다.
  - 대신 **2 워커로 `player.spec.js` 전체를 8번 돌리는 부하 테스트**에서 다른
    현상을 하나 더 봤다: 7번은 11개 전부 통과, **1번은 볼륨과 무관한 테스트
    4개(즐겨찾기·탐색바·다음곡·탐색확정)가 한꺼번에 실패**했다 — 특정 로직 하나의
    버그가 아니라 **병렬 워커의 CPU 경합이 심할 때 이 스위트 전반의 타이밍 여유가
    얇다**는 쪽에 더 가까워 보인다. 실패 상세를 셸 출력 자르기(`tail`)로
    놓쳐서 정확한 원인까진 못 갔다.
  - **결론: 이번 CI 실패의 정확한 방아쇠는 못 찾았다.** `gh run rerun`으로
    재실행하니 바로 통과했다. 코드를 추측으로 고치지 않고 이 사실만 남긴다 —
    다음에 또 이런 종류가 나면 이번처럼 trace를 먼저 받아서 실측할 것.
  - **후속 결정(팀장, 2026-09-09): CI에서만 워커 1로 내린다.** `playwright.config.js`에
    `workers: process.env.CI ? 1 : undefined` — 로컬은 그대로 병렬. **이건 버그
    수정이 아니라 환경 결정이다: 근본 원인을 찾아서 없앤 게 아니라, 원인을 못
    찾았으니 그 원인이 발현할 조건(워커 경합) 자체를 없앤 것이다.** 볼륨과
    무관한 테스트 4개가 한꺼번에 흔들린 모양이 특정 로직 결함보다는 워커 경합
    쪽 설명에 훨씬 들어맞고, 이 저장소는 코드 3,286줄·테스트 파일 7개라 워커
    1로 잃는 CI 시간이 1분 남짓뿐이라 판단했다(로컬 실측: 병렬 29초 vs 워커1
    69초). **무작위로 빨간불이 뜨는 게이트는 사람이 "또 그거네"로 재실행을
    누르게 만들고, 그러면 진짜 결함이 났을 때도 똑같이 재실행당한다** — 그
    손해가 1분보다 크다고 봤다. **되돌릴 수 있는 실험이다** — 워커 1로 내린
    뒤에도 같은 흔들림이 또 나오면 경합이 원인이 아니라는 새 증거이니 그때
    다시 판다. `trace: 'retain-on-failure'`는 계속 켜둔다 — 이번에 그게 있어서
    추측을 면했다.
  - 🔴 **진단 코드가 스스로 가짜 불일치를 만든 사례, 다시 강조.** 값이 서서히
    변하는 것(페이드·애니메이션)을 검사할 때 `waitForFunction(v > 임계값)`처럼
    "문턱을 넘었는지"로 판정하면, 목표값이 그 문턱을 지나가는 도중에 걸려서
    없는 버그를 만든다. **`toBeCloseTo`로 최종값에 폴링**하거나 애니메이션이
    끝났다고 알 수 있는 별도 신호를 볼 것 — 이 저장소만이 아니라 값이 흐르는
    UI(볼륨 페이드, 스크롤 등)를 테스트하는 모든 곳에서 반복될 수 있는 함정이다.
- **GitHub Actions 액션 SHA를 추측해서 적는 것.** `upload-artifact@v4`의 SHA를 커밋
  전에 손으로 짐작해 적었다가(`...fa9`), `gh api repos/<org>/<repo>/git/refs/tags/<태그>`로
  실제 태그가 가리키는 커밋과 대조해보니 마지막 자리가 틀려 있었다(`...fa02`가 맞음).
  **SHA는 항상 `gh api`로 대조한 뒤에 적는다** — 틀린 SHA는 그 스텝이 그냥 실패할
  뿐이라 눈에 띄긴 하지만, 커밋에 들어가기 전에 잡는 게 맞다.
- **주소·오리진 문제를 의심할 때 대조군 없이 새 쪽만 보는 것 — 그리고 대조군
  결과를 결론으로 착각하는 것.** 2026-09-10에 `incheon.edutogether.kr`로
  되돌릴지 확인하며, 그 오리진에서만 실제 기능(학교 검색)을 눌러 App Check
  토큰 교환 403·검색 멈춤을 봤다. 거기서 멈췄으면 "도메인이 문제"로 결론
  내렸을 것이다. **지금 쓰고 있는 `.web.app`에도 같은 테스트를 돌려보니
  똑같이 403·멈춤이 나왔다.**
  - **두 오리진이 다르게 반응한다 → 도메인 문제다.** 9/9 AI Ways 사고가 이
    경우였다(403 대 204로 갈렸다).
  - **두 오리진이 똑같이 실패한다 → 도메인 문제는 아니다, 딱 거기까지만
    말할 수 있다.** 원인은 두 갈래로 남는다: ① 자동화(헤드리스가 invisible
    reCAPTCHA를 못 뚫는 것 같은)의 한계일 수도 있고, ② **양쪽 다 진짜로
    고장났을 수도 있다**(그러면 실사용자 전원이 못 쓰는 상태다). **이 둘을
    가르는 건 대조군이 아니라 사람이 실제 기기로 해보는 것뿐이다.**
  - **처음엔 이 구분을 놓쳐서 "테스트 방법의 한계"를 결론으로 적었다.** 바로
    다음 날 대표님이 실제 휴대폰으로 `incheon.edutogether.kr`에서 검색해도
    "네트워크 오류"가 났다 — ②였다. **"자동화 한계"는 결론이 아니라 아직
    확인 못 한 상태의 이름이다.** `COMMON_STANDARDS.md` §21-4와 같은 취지.
- **"페이지가 뜬다"를 "앱이 된다"로 착각하는 것 — `link-healthcheck.yml`이
  9/9 01:58부터 계속 초록불이었는데 AI Ways Incheon의 학교 검색은 그날부터
  실사용자 전원이 못 쓰는 상태였다.** 정적 HTML만 보는 검사는 백엔드·인증
  장애를 구조적으로 못 잡는다(`COMMON_STANDARDS.md` §21-5). 그래서 백엔드가
  있는 앱은 **실제로 하는 일 하나를 인증 없이 불러서, 정확히 그 실패 코드가
  오는지**까지 봐야 한다(2026-09-10 `check_function` 추가, AI Ways의
  `searchSchool`을 인증 없이 호출 → 정확히 `401 app_check_missing`이어야
  통과). **아무 응답이나 받는 것과 다르다** — 이 정확한 코드가 온다는 것
  자체가 함수가 살아 있고 배포됐고 오리진이 허용됐고 App Check 검사까지
  도달했다는 뜻이다. `!= 500` 같은 배제 방식이 아니라 **허용목록**으로
  판정해야 하고(§21-5), 새로 넣을 땐 일부러 없는 함수 이름·없는 호스트로
  바꿔 실제로 빨간불이 뜨는 것까지 확인한다(§21-2). **엔드포인트·기대
  응답을 확신 있게 모르면 억지로 만들지 않는다** — Poster Studio/CLASSCADE/
  Be a Googler도 백엔드가 있지만 이 저장소 밖에서 정확한 계약을 알아낼 수
  없어서 그 셋은 지금 추가하지 않았다.
- 🔴 **"검사가 초록불이다"와 "실사용자가 쓸 수 있다"는 다른 층이다 — 위
  기능 검사도 여기서 예외가 아니다.** 2026-09-10에 AI Ways `searchSchool`
  기능 검사(정확히 401 `app_check_missing`이 오는지)를 넣었는데, 이 검사가
  초록불인 것은 **"그 함수는 살아있고 App Check 검사까지 도달한다"**만
  증명한다. 실제로 대표님 휴대폰에서 학교 검색이 "네트워크 오류"로 안 됐을
  때도 이 함수는 내내 정상이었다 — 문제는 **브라우저 쪽**이었다. App Check가
  한 번 403을 받으면 그 브라우저가 **24시간 동안 재시도조차 하지 않는**
  클라이언트 캐싱이 있었고, 시크릿 창(그 기록이 없는 상태)으로는 바로
  됐다. **자동 검사도, 서버 로그도, "함수가 정상이다"까지만 보여준다 —
  "이 사람의 이 브라우저에서 된다"는 사람이 직접 해봐야 안다.** 다음에
  "검사는 초록인데 사용자는 못 쓴다"는 신고가 오면 이 줄이 답이다(경위는
  `_docs/archive/history.md`).
- **CI가 `npx playwright install --with-deps chromium`에서 `Hash Sum
  mismatch`로 실패하면 우리 쪽 문제가 아니라 Google Chrome apt 저장소가
  깨진 것부터 의심할 것.** 2026-09-10에 5연속·15분간 실패했다. **처음엔
  "외부 장애다"로 결론짓고 넘어가려 했는데, 그건 가장 편한 결론이라 가장
  의심해야 하는 것이었다**(org 공통 기준으로 올라감) — Voice Cinema가 자기
  PC에서 같은 파일을 직접 받아 해시를 계산해 CI 로그의 두 값과 정확히
  일치하는 것까지 확인하고 나서야 진짜 외부 원인(Google이 그때 내려주던
  인덱스 자체가 깨져 있었음)으로 확정됐다.
  - **1차 대응(apt 캐시 삭제 후 재시도)은 임시방편이었다(헌법 §7).** 깨진
    게 로컬 캐시가 아니라 원격 인덱스 자체라, 캐시를 지우고 다시 받아도
    **같은 깨진 파일을 다시 받는다** — 실제로 재시도해도 똑같이 실패하는
    것으로 확인됨. 그다음 그 저장소 목록 파일만 지우고 재시도하는 것도
    통과는 시켰지만, apt를 계속 타는 한 다음에 Google이 또 흔들리면
    똑같이 막힌다.
  - **근본 해법: apt를 아예 안 타게 함.** `--with-deps`가 시스템 공유
    라이브러리를 apt로 까는 유일한 부분이라 뗐다 — `npx playwright install
    chromium`만 쓰면 브라우저 바이너리를 Playwright 자체 CDN에서 HTTPS로만
    받는다. GitHub `ubuntu-latest` 러너에는 필요한 라이브러리가 이미
    대부분 있어서 이것만으로 25개 테스트 전부 통과했다(실측: 로그에
    `dl.google` 참조 0건, `Running 25 tests` / `25 passed` 확인). 여기에
    `actions/cache`로 `~/.cache/ms-playwright`를 Playwright 버전 기준으로
    캐시해서, 캐시가 맞으면 브라우저 설치 스텝 자체가 스킵된다(실측:
    첫 실행은 `Cache not found`로 새로 받고 `Cache saved`, 그다음부터는
    `cache-hit: true`로 다운로드 자체가 없음).
  - **재시도 로직은 뺐다.** apt를 안 타게 되면서 그 재시도가 지키던 경로
    자체가 없어져 의미 없는 코드가 됐다(헌법 §7 — 의미 없는 코드를 남기지
    않는다).
  - **재현 없이 "외부 장애"로 결론짓지 말 것.** 남의 시스템을 탓하기 전에
    직접 받아서 대조한다. 그리고 외부 장애가 맞더라도 **게이트를 낮추지
    않는다** — 우회해야 하면 같은 검사를 다른 곳에서 실제로 돌려 근거를
    만들고, 우회 사실과 대체 근거를 기록하고, 풀리면 정규 경로로 다시
    내보낸다.

## 배경작업 재개 지점 (§19)

지시가 없을 때 돌리는 배경작업과, 지금 어디까지 했는지 한 줄. **다음 세션은 여기서부터** 이어간다.

- **지금 할 일**: README·문서가 지금 구조(리액트 전환 이후)와 맞는지 점검. 대표님 팀이 9/18까지 동료평가 중이라 평가자가 문서·저장소를 본다.
- **진행 상황(2026-09-09, 1차 점검 완료, 커밋 `3d088c0`)**:
  - README.md/AGENTS.md/CLAUDE.md/`.claude/rules/app.md`/`_docs/intents/README.md`의
    내부 링크·백틱 파일 경로를 전수 대조 — `.github/workflows/` 실제 파일 8개와
    문서상 언급 대조, 깨진 링크나 없는 파일 참조는 없었다.
  - `_docs/ops/HANDOFF.md`의 "이 폴더의 CLAUDE.md" 참조가 파일 이동 전 문장이
    그대로 남아 있던 것을 발견해 고침(`3d088c0`).
  - `sync-check.yml`/`player-smoke-test.yml`이 오늘 낮 CLAUDE.md 축약(226→102줄,
    `6cc3caf`) 때 유실 검사 키워드 목록에서 빠뜨려 문서에서 사라졌던 것을 발견,
    `font-coverage-check.yml`/`budget-alert-check.yml`(원래도 미문서화)과 함께
    AGENTS.md "배포 경로"에 8종 전부 정리(`3d088c0`).
  - `react-typescript-migration` intent의 `status: draft`를 `done`으로 바꿨다
    (팀장 승인, `0f91307`) — "상태는 Bumm님만"이 막는 건 세션의 자기 제안 자기
    승인이지, 이미 배포·감사까지 끝난 사실을 기록하는 것까지는 아니라는 판단.
    intent가 이거 하나뿐이라 다른 것은 훑어볼 대상이 없었다.
  - `_docs/archive/history.md`의 날짜·이벤트 서술을 `git log`와 6곳 대조(저장소
    생성일, GitHub Pages→Actions 전환, Firebase Hosting 이전, 배포 대상 긍정목록
    전환, 로딩화면 버그 수정 2건) — **전부 커밋 시각·내용과 정확히 일치**, 고칠 것
    없었다. README.md의 명령 블록(`npm ci`/`dev`/`build`/`test`/`lint`)도
    `package.json`의 실제 `scripts`와 대조 — 일치.
  - **2026-09-09, `D:\Projects\_shared\DOC-STANDARD.md`(문서 표준) 도입에 맞춰
    전면 점검·정리 완료**: 필수 문서 7종 확인, `_docs/CHANGELOG.md` 신설(오늘부터
    기록 시작), `HANDOFF.md`→`handoff.md`·브리프 한글 파일명→영문 정리,
    `AGENTS.md`의 절차성 내용("운영 — 배포·비용·롤백")을 `_docs/ops/deploy-and-billing.md`로
    이동(옮기기 전후 문단 단위 대조로 사실 유실 없음 확인). `CLAUDE.md`(113줄)는
    표준의 완화된 상한(120줄, "한두 줄 넘는 건 그대로 둔다")보다 작아 손대지
    않았다 — 다만 내용도 다시 읽어 "지금 유효한 규칙 아닌 서술"이 섞여 있는지는
    확인했고 없었다.
  - **2026-09-09, 헌법 §8/COMMON_STANDARDS §20("문서와 기록") 반영 완료**:
    프리즈 태그 38개 중 주요 시점 8개(생성·카드배치확정·Actions전환·Firebase이전·
    첫100점감사·React전환직전고정점·React전환후100점감사·최신)에 `gh release
    create`로 릴리스노트 부착(https://github.com/edutogether/portal/releases,
    태그 자체는 안 옮김). README.md에 "어떻게 만들어졌는가" 절 추가 — 이전
    세션 요약에만 있던 "computed style 11,592개" 같은 이 저장소에서 재검증
    못 하는 수치는 넣지 않고 근거 문서 링크로 대체했다. AGENTS.md에 "지금까지
    모든 변경은 main 직접 커밋(PR 0건)"을 명시 — PR 트리거 워크플로우 설명
    자체는 틀리지 않았지만 실사용 여부가 안 적혀 있어 오해 소지가 있었다.
    `thumbnail-briefs/`의 영문 파일명 판단(사람이 여는 행사 인쇄물이 아니라
    저장소 내부 작업 지시문)은 유지.
  - **2026-09-09, CI Playwright 워커를 1로 내림(팀장 결정, `311b743`)** —
    "CI 플레이키 2차 발생" 조사(위)에서 근본 원인을 못 찾은 뒤 팀장이 내린
    후속 결정. 버그 수정이 아니라 비결정성 제거를 위한 환경 결정이고, 로컬은
    병렬 유지. CI 로그로 실제 "using 1 worker" 확인. 같은 흔들림이 다시
    나오면 경합이 원인이 아니라는 새 증거이니 그때 다시 조사할 것 — 이게
    재발 감시 지점이다.
  - **2026-09-09, `COMMON_STANDARDS.md` §21(빈 게이트) org 공통 기준 반영** —
    portal의 CI 조사가 §21-3의 근거로 들어갔고, 팀장이 이 저장소 검사들도
    §21-1(대상 0건이면 통과가 아니라 실패)로 훑어보라고 해서 전수 점검했다.
    **`check-font-coverage.py`에서 실제로 하나 찾았다** — "빠진 글자 0개"를
    성공 기준으로 삼는 검사라 "검사할 글자 0개"일 때도 똑같이 통과했다.
    지금까지는 `tests/font-text.spec.js`의 고정 문자열('재생할 수 없습니다')이
    우연히 항상 붙어서 안 터졌을 뿐, 설계상 막혀 있던 게 아니었다. 의미 있는
    글자 0개면 명시적으로 실패하도록 고치고, 일부러 빈 덤프로 실패 재현 후
    정상 빌드로 재확인했다(`9753ac3`). `check-inline-script.py`(dist/ 존재
    여부 명시 검사)와 Playwright 테스트들(루프 앞에 전부 고정값 대조가 있어
    0건이면 그 자리에서 먼저 실패)과 `link-healthcheck.yml`(앱 7개 하드코딩,
    동적 목록 아님)은 이미 안전했다.
  - **2026-09-09, §21-1 2차 점검(대표 지시로 전 앱 재점검, 세 범주 지정)** —
    Poster Studio가 자기 저장소에서 4건을 더 찾은 것을 계기로, 이 세 범주로
    다시 훑었다:
    1. **시각/레이아웃 대조**(`tests/visual-snapshot.spec.js`) — 기준 이미지를
       일부러 지우고 실행해 실제로 실패하는지 실측 확인(Playwright의
       `toHaveScreenshot()`은 기준 없으면 기본적으로 실패, `--update-snapshots`
       없이는 자동 생성 안 함). 이 파일 자체가 CI에서는 애초에 안 돈다(`--grep-invert`,
       처음부터 로컬 전용으로 설계 — 리눅스 러너엔 플랫폼별 기준 이미지가 없어서).
    2. **파일·경로 훑는 검사** — `check-inline-script.py`(안전, 앞서 확인)
       외에 새로 훑을 스크립트는 없었다. `tsc -b`(빌드 게이트)는 `src/`가
       없으면 `TS18003 No inputs were found`로 하드 실패하는 걸 실측 확인
       (`src/`를 통째로 옮겼다 되돌려서 확인). ESLint 자체의 "대상 0개면
       0문제로 통과"하는 성질은 확인했지만, 이건 이 저장소가 만든 검사가
       아니라 도구 자체의 범용 동작이라 손대지 않았다 — src/가 사라지면
       그 전에 `tsc -b`가 먼저 하드 실패해서 실질적으로 도달 불가능한
       경로이기도 하다.
    3. **배포 산출물 검사** — `check-inline-script.py`가 `dist/index.html`·
       `dist/404.html` 존재를 명시적으로 확인(기존), `sync-check.yml`의
       `cmp`도 파일 없으면 실패. `npm run build`(`tsc -b && vite build`)
       자체가 `&&`로 묶여 있어 앞 단계 실패 시 뒤가 안 돈다.
    4. `continue-on-error`/`|| true`/`|| exit 0` 같은 실패를 삼키는 패턴이
       워크플로우·스크립트 어디에도 없는지 전수 grep — 0건.
    **결론: 추가로 고칠 것을 찾지 못했다.** 억지로 만들지 않고 그대로 보고.
  - **2026-09-09, 남은 두 항목 점검 완료**: `_docs/ops/handoff.md` 본문은
    이미 맨 위 배너가 "2026-08-14 스냅샷, 최신은 CLAUDE.md 확인"이라고 못
    박고 있어 본문 안 오래된 절차(`node .claude/static-server.js`, 이제
    존재 안 함)를 추가로 손대지 않았다 — 아카이브 문서의 목적이 그 시점
    기록이지 최신화가 아니기 때문. CLAUDE.md/AGENTS.md/app.md 세 문서
    사이 사실 대조(테스트 개수 25, Firebase 계정명, "주소가 박힌 다섯 곳"
    목록의 실제 URL 일치 여부)는 전부 일치 — 어긋난 곳 없었다.
  - 나머지 30개 프리즈 태그(일 단위 세부 조정)는 릴리스노트를 안 붙였다 —
    필요하면 그때 추가.
  - **다음에 확인할 것**: 지금까지 몇 차례 점검을 거쳐 눈에 띄는 문제는
    대부분 찾아 고쳤다. 다음 세션은 새로 바뀐 부분(오늘 이후 커밋) 위주로
    가볍게 훑는 것으로 충분할 것 — 처음부터 전수 재검사할 필요는 없다.
  - **2026-09-10, AI Ways 주소 정리 완료**: 결국 `incheon.edutogether.kr`
    (정식 서브도메인)로 확정, 다섯 곳(`src/data/apps.ts`, `CLAUDE.md`,
    `tests/portal.spec.js`, `tests/locked-geometry.spec.js`,
    `.github/workflows/link-healthcheck.yml`) 전부 갱신. **원인은 도메인도
    코드도 아니었다** — 서버 Functions는 내내 정상이었고, App Check 403을
    한 번 받은 브라우저가 24시간 재시도를 안 하는 클라이언트 캐싱 때문에
    특정 기기에서만 안 되는 것처럼 보였다. `ai-ways-incheon.web.app`은
    대비용으로 허용목록에 남겨두되(AI Ways가 처리), 포털이 보여주는 주소는
    정식 주소 하나. 경위 전문은 `_docs/archive/history.md`, 원칙은 위
    "자주 틀리는 것"에 남겼다.
  - **2026-09-10, CI 수정(a7af784) §21-2 3단계 검증 완료, 스트림 종결**: ①
    캐시 히트(31bdc1d 실행 — 설치 스텝 `skipped completed`) ② 캐시 미스
    (`gh cache delete`로 직접 지운 뒤 재실행 — 184.3MiB 실제 다운로드 로그
    확인) ③ **테스트를 일부러 깨뜨렸을 때 CI가 실제로 빨간불이 되는지**
    (`tests/portal.spec.js`를 일부러 깨서 push, b6fd9ce/run 34387229366 —
    test job 실패·deploy job 스킵 확인, 즉시 원복 후 재푸시해 25개 전부
    통과·배포 성공까지 재확인, 38b3fb6). ②·③은 캐시를 미리 지워둔 덕에
    같은 실행 하나로 동시에 증명됐다. 팀장에게 결과와 함께 OG 이미지
    자기호스팅 URL 표(각 앱 `/og.jpg` 제안)를 재전달해 이 스트림을
    닫았다. AI Ways/OG/CI 세 갈래 작업 전부 종결.
  - **2026-09-10, `font-coverage-check.yml`·`player-smoke-test.yml`도 apt 의존
    제거(팀장 지시, 29e9079)**: `deploy.yml`에 적용한 방식(`--with-deps` 제거 +
    `actions/cache`)을 이 두 워크플로우에도 맞췄다 — Voice Cinema의 진단대로
    애초에 apt 자체가 필요 없었다는 결론이 세 워크플로우 전부에 똑같이
    적용된다. 별도 브랜치 PR(#3, Voice Cinema 방식)에서 §21-2 세 단계(캐시
    히트/미스/두 워크플로우 각각 의도적 실패→빨간불)를 전부 실측 확인 후
    병합 없이 닫고 main에 직접 반영. 검증 중 `font-coverage-check.yml`의
    실패 자동 이슈 생성이 실제로 작동해 이슈 #4가 생겼는데, 검증용이었다는
    사유를 남기고 닫았다 — **§21-2 검증이 부작용(실제 이슈·알림)을 만들 수
    있다는 것을 놓치지 말 것**, 다음에도 실패 시 이슈/알림을 만드는 워크플로우를
    일부러 깰 땐 그 부작용까지 같이 정리해야 한다.
  - **2026-09-10, org 파이프라인 첫 완주(문서정리→종합감사100→배포→프리즈 태그,
    팀장 지시)**: ① 문서정리 — `CLAUDE.md`의 AI Ways 경위 서술이 app.md·
    archive/history.md와 중복돼 있던 것 정리(113→110줄, 문단 대조로 유실 0줄
    확인), `_docs/ops/deploy-and-billing.md`의 롤백 소요시간(1.7분→실측
    2분 안팎) 갱신. ② 종합감사 — §7대로 Opus(별도 Agent 호출, 6항목)와 이
    세션(Sonnet, 4항목)이 독립 조사, 범위는 마지막 100점 태그(9/9 13:00)
    이후 변경분 전체. 결함 4건 발견 즉시 수정: `link-healthcheck.yml`
    `check_function`이 실패 시 남의 프로젝트 응답 본문을 공개 이슈에 그대로
    실은 것(200자 절단), 같은 함수가 상태코드·본문을 curl 두 번으로 따로 받아
    간헐 장애 중 서로 다른 응답을 섞을 수 있던 것(curl 1회로 통합), 세
    워크플로우의 Playwright 버전 확인이 명령치환 실패를 echo가 가려 빈 값으로
    캐시 키를 만들 수 있던 것(빈 값이면 즉시 실패), og:image 메타 태그를
    검증하는 테스트가 0개였던 것(추가 — 파일을 실제로 지워 실패를, 복구해
    통과를 확인). **수정 커밋 `aea473b` 배포 중 eslint globals에 `URL`이
    빠진 걸 놓쳐 CI 린트가 한 번 빨간불 났다** — 로컬에서 `npm run lint`를
    먼저 안 돌리고 push한 게 원인, `455a751`로 즉시 잡음(다음엔 워크플로우
    파일이 아닌 테스트 코드를 새로 추가할 때도 lint/build/test를 커밋 전에
    반드시 로컬로 먼저 돌릴 것). 최종 10/10 100점, 미해결 1건(OG 이미지
    갱신을 각 앱 자기호스팅 사본과 묶는 검사 부재)은 6개 저장소에 걸친
    정책이라 도구를 임의로 만들지 않고 팀장 판단으로 올림. ③ 배포는 이미
    수정 커밋이 배포·라이브 확인까지 끝난 상태였고, ④ `portal-freeze-
    20260910-audited-100` 태그를 그 커밋에 찍고 GitHub 릴리스 부착,
    `git checkout <태그>` → `git diff main <태그>`로 **차이 0건** 실측
    확인 후 `git checkout main`으로 원위치까지 마쳤다.
  - **2026-09-10, OG 이미지 원본-배포본 대조 추가(팀장 판단, `c7a31ee`)** —
    위 미해결 1건에 대한 팀장 판단: 6개 저장소에 도구를 심지 않고, 포털이
    **자기 저장소 안에서만** `actions/checkout` 추가해 원본과 각 앱 라이브
    `og:image`를 curl+sha256으로 대조(`check_og_image`, `link-healthcheck.yml`).
    QUIZ TOGETHER는 외부 서비스라 제외. §21-2 검증(포스터 원본 1바이트 변조 →
    실제 라이브 엔드포인트로 불일치 확인 → 원복 → 재확인)까지 실측 완료. 실제
    첫 실행에서 Poster/Voice/CLASSCADE/Portal 4곳 일치, AI Ways·Be a Googler는
    아직 미적용으로 불일치(버그 아님, 정상 신호) — 기존 헬스체크 실패 이슈(#5)
    에 코멘트로 남김. **이건 새 프리즈 태그를 만들지 않는다**(팀장 지시,
    "기존 태그 뒤의 정상 변경") — CHANGELOG에만 기록, 다음 감사 라운드에서
    함께 태그.
  - **2026-09-10, OG 대조 검사의 오탐 수정(팀장 지적, `f7fa134`+`abcabbc`)** —
    Be a Googler가 "태그 없음"으로 잡혔는데 실제로는 있었다. **§21-6 사례**:
    읽기 실패(추출기 버그)와 진짜 부재를 같은 결과로 보고하고 있었다. 겹친
    버그 둘: ① 속성 순서·따옴표 종류 취약 ② **grep이 한 줄씩만 봐서 여러
    줄에 걸친 `<meta>` 태그를 원천적으로 못 찾음**(진짜 원인 — 합성 한 줄
    HTML로만 재현 테스트를 해서 처음엔 놓쳤고, 실제 라이브 HTML을 직접
    받아보고서야 드러났다). **재현 테스트를 합성 데이터로만 하면 실제
    데이터의 형태(여러 줄 포매팅 등)에서만 나오는 버그를 놓칠 수 있다** —
    가능하면 실제 대상으로도 한 번은 돌려볼 것. `FETCH_FAIL`/`NOTAG`/
    `MISMATCH`를 서로 다른 메시지로 가르는 것과 줄바꿈을 공백으로 누른 뒤
    검색하는 것 둘 다로 고쳤다. 재검증 후 Be a Googler는 실제로 일치,
    AI Ways는 여전히 정확히 불일치(진짜 미적용) — 팀장에게 보고 완료.
  - **2026-09-10, `deploy.yml` deploy 잡에 경로 필터 추가(COMMON_STANDARDS
    §23, `95403be`)** — `**/*.md`·`_docs/**`·`.claude/**`만 바뀐 커밋은
    deploy를 건너뛴다(test 잡은 그대로 항상 돈다). **1차 시도는 뒤집어
    확인하다 실제로 틀린 걸 잡았다** — `dorny/paths-filter`의 기본
    predicate-quantifier(`'some'`)는 `!` 패턴을 독립 대안으로 OR 취급해서,
    목록에 넣은 순수 `'**'` 패턴 하나만으로 이미 전부 매칭돼버려 제외가
    전혀 안 걸렸다. **공식 문서를 안 읽고 "이렇게 되겠지"로 짐작했던 게
    원인** — `predicate-quantifier: 'some-with-excludes'`를 추가해야
    `!` 패턴이 진짜 배제로 동작한다. 문서 전용 커밋으로 다시 확인해
    `Filter deploy = false` 로그까지 보고서야 통과시켰다. **새 액션을
    처음 쓸 때 동작 방식을 짐작하지 말고 README를 먼저 읽을 것** — 이번엔
    다행히 §21-2(뒤집어 확인) 절차 자체가 이 틀림을 그 자리에서 잡아줬다.
  - **지금 대기 중(팀장 지시, 2026-09-10)**: AI Ways 세션에 팀장이 직접
    원본 적용을 지시했다(Be a Googler는 이미 적용 확인됨). 이 세션은 새
    작업을 만들지 않고 대기하며, **다음에 `link-healthcheck.yml`이 돌 때**
    (매일 09:00 KST 자동, 또는 그 전에 `gh workflow run
    link-healthcheck.yml`로 수동 확인) AI Ways의 OG 이미지 대조가 초록불로
    바뀌었는지만 확인해 팀장에게 한 줄로 보고한다.
